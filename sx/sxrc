#!/usr/bin/env sh

export XDG_SESSION_TYPE=x11

autorandr --change || monitor
bstatus -l &
hsetroot
pulsemixer --unmute
sxhkd &
xbanish &
xcompmgr &
xkbcomp "$XDG_CONFIG_HOME/xkb" "$DISPLAY"

exec herbstluftwm -l
