#!/usr/bin/env sh

autorandr --change
bstatus -l &
hsetroot
nm-applet &
picom --config /dev/null -b
pulsemixer --unmute
sxhkd &
wal -l &
xbanish &
xkbcomp "$XDG_CONFIG_HOME/xkb" "$DISPLAY"

exec herbstluftwm -l
