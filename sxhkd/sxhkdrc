mod5 + ctrl + r
    pkill -SIGUSR1 sxhkd

XF86AudioMute
    volume =
XF86Audio{Lower,Raise}Volume
    volume {-,+}
XF86MonBrightness{Down,Up}
    brightness {-,+}

XF86PowerOff
    systemctl suspend
super + XF86PowerOff
    systemctl reboot
super + shift + XF86PowerOff
    systemctl poweroff

{mod5,super} + Return
    "$TERMINAL" -e tmux new \; splitw -h
{mod5,super} + f
    dmenu_run
{mod5,super} + t
    libreoffice ~/data/notes/tracker.ods
super + {v,space}
    dunstctl {history-pop,close}

mod5 + S
    pkill sswm
{mod5,super} + d
    sswm close
{button2,mod5 + l,super + s}
    sswm last
