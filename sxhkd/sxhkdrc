mod5 + ctrl + r
    pkill -SIGUSR1 sxhkd

XF86AudioMute
    pulsemixer --toggle-mute
XF86Audio{Lower,Raise}Volume
    pulsemixer --change-volume {-,+}4
XF86MonBrightness{Down,Up}
    lux -{s,a} 8%

mod5 + c
    st
mod5 + f
    launch
mod5 + D ; {p,r,s}
    {poweroff,reboot,systemctl suspend}
mod5 + t
    libreoffice ~/data/notes/tracker.ods

{button2,mod5 + m}
    /usr/share/doc/herbstluftwm/examples/maximize.sh
button{8,9}
    "$XDG_CONFIG_HOME/herbstluftwm/cycle.sh" {+,-}
