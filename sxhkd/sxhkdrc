mod5 + ctrl + r
    pkill -SIGUSR1 sxhkd

XF86AudioMute
    pulsemixer --toggle-mute
XF86Audio{Lower,Raise}Volume
    pulsemixer --change-volume {-,+}4
XF86MonBrightness{Down,Up}
    lux -{s,a} 8%

{mod5,super} + Return
    st
{mod5,super} + f
    launch
mod5 + shift + {1,2,3}
    systemctl {poweroff,reboot,suspend}
{mod5,super} + t
    libreoffice ~/data/notes/tracker.ods

@Super_L
    sswm pop
super + Tab
    sswm cycle+
mod5 + S
    sswm quit
{mod5,super} + d
    sswm delete
{mod5,super} + a
    sswm swap
{mod5 + l,super + s}
    sswm jump
mod5 + {H,L}
    sswm ratio{-,+}
{button2,mod5 + m,super + v}
    sswm maximize
