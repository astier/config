mod5 + ctrl + r
    pkill -SIGUSR1 sxhkd

XF86AudioMute
    volume =
XF86Audio{Lower,Raise}Volume
    volume {-,+}
XF86MonBrightness{Down,Up}
    brightness {-,+}

{mod5,super} + Return
    "$TERMINAL"
{mod5,super} + f
    dmenu_run
mod5 + shift + {1,2,3}
    systemctl {poweroff,reboot,suspend}
{mod5,super} + t
    libreoffice ~/data/notes/tracker.ods
super + {v,space}
    dunstctl {history-pop,close}

mod5 + S
    pkill sswm
{mod5,super} + d
    wmctrl -c :ACTIVE:
{button2,mod5 + l,super + s}
    wmctrl -ia "$(wmctrl -l | sed -n 2p | cut -d' ' -f1)"
